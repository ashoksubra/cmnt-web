# Agent notes — cmnt-web

- This is **Path A**: TypeScript core, SVG score, CSS school theming.
- Desktop JAR at `../CMNT/CMNT-Notation-Studio-source` is the **layout gold standard** until parity tests pass. Prefer reading it; do not modify it from this workspace unless the user asks.
- GitHub remote: `ashoksubra/cmnt-web`. Prefer clean commits; never force-push `main` unless the user explicitly asks.
- At the end of substantial work, if commits are ahead of `origin` (or uncommitted Iteration deliverables exist), **prompt the user** for a clean commit + push. Do not push without confirmation unless they already asked to push.
- Keep Iteration 1 focused on core parity; defer UI chrome until Iteration 3.
